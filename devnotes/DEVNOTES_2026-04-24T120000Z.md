<!--
DEVNOTES snapshot
Created: 2026-04-24T12:00:00Z
Purpose: Point-in-time snapshot of migration state before performing large repo-wide updates.
-->

# DEVNOTES Snapshot — 2026-04-24T12:00:00Z

Summary:

- Purpose: snapshot the current migration state for `src/lib/function` and ongoing Svelte 5 migration work prior to larger, repo-wide changes.
- Created by: automated agent (pair-programmer flow).

Completed/recorded changes (function-focused):

- Implemented TypeScript modules under `src/lib/function`: `sprites.ts`, `particle.ts`, `pool.ts`, `ml-camera.ts`, `overlay.ts`, `emotion.ts`.
- Introduced `coordinator.ts` (formerly `particleBridge`) exposing `mountCoordinator()` and lifecycle APIs: `initCoordinator`, `startCoordinator`, `stopCoordinator`, `clearMBTIParticles`.
- Added `tsconfig.function.json` for focused TypeScript checks targeting `src/lib/function`.
- Added ambient typings (`types/ambient.d.ts`) to reduce noise during focused checks.
- Dedupe in `pool.ts` and added SSR-null guards in `ml-camera.ts`.
- Wired `Canvas.svelte` to call `mountCoordinator(videoEl)` on mount and cleanup on destroy.
- Focused TS check (`npx tsc --noEmit -p tsconfig.function.json`) passed without errors.

Outstanding items (high priority):

- Finish `coordinator` socket handlers parity with legacy `particleFull.js` (e.g. `spawn_particles`, `session_reset`, `lucky_color`).
- Run repo-wide TypeScript check (`npm run check`) and triage errors outside `src/lib/function`.
- Run dev server (`npm run dev`) to validate runtime (camera, MediaPipe, socket events).
- Remove legacy `particleFull.js` after behavior parity verified and backups committed.
- Audit `src/lib/runes` (external agent currently fixing Svelte 5 issues there) and re-run checks once fixed.
