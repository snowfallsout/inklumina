<!--
檔案說明：
- 記錄 2026-05-02 對 `particleFull.js` 的最終刪除。
- 本輪依據既有基線，將 `static/*` 確立為唯一受保護的原始功能 reference，移除未被引用的 legacy monolith。
-->

# DEVNOTES_2026-05-02T211000Z

## 背景

在前一輪之後，`src/lib/function` 根目錄只剩一個 historical JS 檔：

- `particleFull.js`

它沒有任何活躍引用。更重要的是，目前專案已經有明確的 reference baseline：

- `static/display.html`
- `static/mobile.html`
- `static/server.js`

因此 `particleFull.js` 已不再是必要的參考來源。

## 本輪決策

### A. 刪除 `particleFull.js`

刪除理由：

1. workspace 中沒有任何活躍引用
2. 受保護且明確的 reference baseline 已轉移到 `static/*`
3. 保留 monolith 只會讓 `function/` 持續混雜 active kernel 與 historical legacy JS

### B. 更新 `_CORE_FILES_.md`

同步更新 `_CORE_FILES_.md`，把 `particleFull.js` 從「歷史參考」改為「已移除」，並明確指向 `static/*` 作為 reference baseline。

## 本輪不做

本輪刻意不做以下事項：

- 不改 `function/runtime/*`
- 不改 `static/*` 任何保護 reference 檔案
- 不重命名 `function/runtime`

## 驗證方式

本輪完成後需確認：

- `npm run check` 通過
- workspace 中不再存在 `particleFull.js`
- `src/lib/function` 不再保留任何 historical JS monolith

## 下一步

若本輪穩定，下一步應優先處理：

1. 是否要把 `function/runtime/` 重新命名成更直接的 kernel 目錄
2. 是否要把 `_CORE_FILES_.md` 的內容上移到更靠近 `services/display` 的說明文件中
3. 是否要為剩餘 kernel 檔案補更明確的 boundary 說明